#include <iostream>
#include <queue>
using namespace std; // queue -> std
int main() {
	int n,a,b,t;
   queue<int> q;
   vector<int> terreno;
   cin>>n;;
	for(int i=0; i <n ; i++)
   {
      for (int j = 0; j < n; j++)
      {
         cin>>t;
         terreno.push_back(t);
      }
	}
   cin>>a,b;
}

