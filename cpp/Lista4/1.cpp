#include <iostream>
#include <vector>
#include <iterator>
using namespace std;

int main(){

    int v1[5],j;
    cin>>j;
    vector<int>v2(4);
    
    for(int i=0;i<v.size();i++){
        cin>>v1[i]>>v2[i];
        v1.pushback(j)
        v2.pushback(j)
        cout<<v1[i]<<v2[1]<<endl;
 
   }for(vector<inwt>::iterator it=v.begin(); it!=v.end(); it++)
   {
    cout<< *it<<endl;
   }
   
}